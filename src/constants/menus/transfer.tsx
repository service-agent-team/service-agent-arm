import { ROUTES } from '../routes';
import type { MenuProps } from 'antd';

export const transfer: MenuProps['items'] = [
  {
    key: ROUTES.transfer,
    icon: 'HomeOutlined',
    label: 'Home',
  },
];
